syntax = "proto3";

package lil_wrap;

message WrappedPayload{
    enum Type{
        // TODO: Add actual transaction types here
        STANDARD_TRANSACTION = 0;
        PARTIAL_TRANSACTION = 1;
    }

    int32 version = 1;
    Type type = 2;
    bytes payload = 3;
}